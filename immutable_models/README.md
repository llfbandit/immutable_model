Defines the annotation used by [immutable_models_gen](https://pub.dev/packages/immutable_models_gen) to generate `==` and `copyWith` extensions.

Also contains:
- Equality helper methods.
- Immutable collections.
