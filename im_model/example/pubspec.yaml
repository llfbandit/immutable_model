name: example
description: A sample command-line application.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  im_model:
    path: ../

dev_dependencies:
  lints: ^2.1.1
  build_runner: ^2.4.6
  
  im_model_gen: ^1.0.0
