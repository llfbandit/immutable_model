name: im_model
version: 1.2.1
description: Annotation for immutable model to generate `copyWith` and == (equal) operator.
homepage: https://github.com/llfbandit/immutable_model/tree/master/im_model

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  meta: ^1.15.0               # https://pub.dev/packages/meta

dev_dependencies:
  lints: ^5.1.1               # https://pub.dev/packages/lints

  test: ^1.19.4               # https://pub.dev/packages/test
